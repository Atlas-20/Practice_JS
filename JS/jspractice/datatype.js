//To change any string to number we can write as 
// as if we write '122'then it will give the output as '122' but as string but 
// if put +'122' then it will give 122 as output because now the string '122' have been changed to 
//number 122 and this is what it is //
// But if put +'Ranjan(any name of word)' then it will give output as NaN
//NaN is not a number 
parseInt('ranjanhello')
//It kis also used to change the string to number but sometime it gives NaN too 
100 + ''  //This give output as '100' , now it is a string . So this is used to change number to string .
typeof true//boolean and even for the false it will give boolean 
+true //1  +false  //0